server {
    listen 6767;
    server_name localhost;

    root /usr/share/nginx/html;
    index portfolio.html index.html;

    # files are bind-mounted and edited live on the host:
    # sendfile caches stale file handles, and browsers cache aggressively
    sendfile off;
    add_header Cache-Control "no-cache";
}
